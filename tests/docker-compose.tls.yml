services:
  tls-proxy:
    image: nginx:1.29-alpine
    depends_on:
      keycloak:
        condition: service_started
    ports:
      - "127.0.0.1:8443:443"
    volumes:
      - ./tests/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./tests/certs:/etc/nginx/certs:ro
    networks: [identity]
